<section class="page-shell error-shell">
  <div class="error-card glass-card">
    <span class="eyebrow">500</span>
    <h1>Ocurrio un error inesperado</h1>
    <p>{{message}}</p>
    <a class="button-primary" href="/">Volver al inicio</a>
  </div>
</section>
